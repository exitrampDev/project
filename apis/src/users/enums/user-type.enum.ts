// src/users/enums/user-type.enum.ts
export enum UserType {
  ADMIN = 'admin',
  SUBSCRIBER = 'subscriber',
  BUYER = 'buyer',
  SELLER = 'seller',
  MA_EXPERT = 'm&a_expert', // underscore instead of space (recommended)
}
